// src/utils/NumberUtils.js

const NumberUtils = {
  // Example: Format number with commas
  formatWithCommas: (number) => {
    return number.toLocaleString();
  },

  // Add more number utility functions as needed...
};

export default NumberUtils;